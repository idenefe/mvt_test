<% content_for :title, "MavaTrak"%> <!--sets tab title-->

<%if logged_in?%>
  <h1>logged in as <%=current_user.username%></h1>
  <p><%= link_to "Log Out", logout_path, class: "btn btn-primary btn-lg", method: :delete %></p>
<%else%>
  <div class="container-fluid" id="main-bg">
    <div class="row center-vertical">
      <div class="jumbotron center col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3 bg-2">
        <h2>Welcome&nbsp;to&nbsp;the MavaTech&trade; Inventory&nbsp;Hompage</h2>
        <p><%= link_to "Log In", login_path, class: "btn btn-primary btn-lg" %></p>
      </div> 
    </div>
  </div>
<%end%>